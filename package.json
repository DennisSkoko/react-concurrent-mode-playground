{
  "name": "react-future-of-data-fetching",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "server": "json-server --port 3001 http://jsonplaceholder.typicode.com/db"
  },
  "dependencies": {
    "json-server": "^0.16.3",
    "react": "^0.0.0-experimental-79740da4c",
    "react-dom": "^0.0.0-experimental-79740da4c",
    "react-router-dom": "^5.2.0",
    "react-scripts": "4.0.3"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
